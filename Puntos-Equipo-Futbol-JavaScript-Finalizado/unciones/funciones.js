const g = document.getElementById("g")

g.addEventListener("click",gato =()=>{

const imprimir = (frase) =>{
    document.write(frase);
}

const CalImc = (peso,altura) => {

    return (victoriasTotal * 3 + empatesTotal);
}

    var victoriasTotal = parseInt(prompt("cuantas victorias tubo su equipo: "));
    var empatesTotal = parseInt(prompt("cuantos empates tubo tu equipo: "));
    
    resultado = victoriasTotal * 3 + empatesTotal;

    imprimir("los puntos finales son: "+ resultado + "<br>");

    fal = 1;

    var faltas = fal + Math.floor(Math.random() * 20)
    
    imprimir("faltas: " + faltas)

})
gato();




