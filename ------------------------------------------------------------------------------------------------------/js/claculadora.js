const sumar = (n1,n2) =>{
    return parseInt(n1) + parseInt(n2);
}
const sumar = (n1,n2) =>{
    return parseInt(n1) - parseInt(n2);
}
const sumar = (n1,n2) =>{
    return parseInt(n1) * parseInt(n2);
}
const sumar = (n1,n2) =>{
    return parseInt(n1) / parseInt(n2);
}
const sumar = (n1,n2) =>{
    return parseInt(n1) % parseInt(n2);
}

alert( "que operacion quieres hacer, escoja un numero: ");

let resul = prompt ( " 1:sumar 2:restar 3:dividir 4:multiplicar 5:resto");